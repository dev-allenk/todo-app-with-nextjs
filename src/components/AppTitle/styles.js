import styled from "styled-components";

const AppTitle = styled.h1`
  display: flex;
  justify-content: center;
  margin: 0;
  margin-bottom: 8px;
  padding: 16px;
  color: #fff;
  background: linear-gradient(to right, #4c80f1, #87b1f3);
`;

export default { AppTitle };
