import styled from "styled-components";

const ListWrapper = styled.ul`
  padding: 0;
  height: 60vw;
  max-height: 60vh;
  overflow-x: hidden;
  overflow-y: auto;
`;

export default { ListWrapper };
